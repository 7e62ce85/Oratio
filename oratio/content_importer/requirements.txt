# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# HTTP client
requests==2.31.0
httpx==0.27.0

# RSS parsing
feedparser==6.0.11

# HTML scraping (for MGTOW.tv, Bitchute, etc.)
beautifulsoup4==4.12.3
lxml==5.1.0

# Scheduling (lightweight — we use threading, not celery)
# (no extra dep needed)
